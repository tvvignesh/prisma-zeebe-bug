export interface OAuthProviderConfig {
    /** OAuth Endpoint URL */
    url: string;
    /** OAuth Audience */
    audience: string;
    clientId: string;
    clientSecret: string;
    /** Cache token in memory and on filesystem? */
    cacheOnDisk: boolean;
}
export declare class OAuthProvider {
    private static readonly cacheDir;
    private static cachedTokenFile;
    audience: string;
    url: string;
    clientId: string;
    clientSecret: string;
    useFileCache: boolean;
    tokenCache: {};
    constructor({ 
    /** OAuth Endpoint URL */
    url, 
    /** OAuth Audience */
    audience, clientId, clientSecret, 
    /** Cache token in memory and on filesystem? */
    cacheOnDisk, }: {
        url: string;
        audience: string;
        clientId: string;
        clientSecret: string;
        cacheOnDisk: boolean;
    });
    getToken(): Promise<string>;
    private fromFileCache;
    private toFileCache;
    private isExpired;
    private startExpiryTimer;
}
