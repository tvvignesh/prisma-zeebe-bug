import * as ZB from '../lib/interfaces';
import { ZBClient } from './ZBClient';
interface WorkerConfig {
    id?: string;
    taskType: string;
}
export interface WorkerCtx {
    job: ZB.Job;
    success: (update?: object) => Promise<boolean>;
    failure: (failureMessage: string) => void;
}
export declare function Worker(options: WorkerConfig & ZB.ZBClientOptions & ZB.ZBWorkerOptions | string): (handler: (WorkerCtx: any) => void) => (zbc: ZBClient) => import("./ZBWorker").ZBWorker<ZB.KeyedObject, ZB.KeyedObject, ZB.KeyedObject>;
export {};
