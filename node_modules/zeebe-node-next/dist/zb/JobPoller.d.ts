/// <reference types="node" />
import { EventEmitter } from 'events';
import { ZBLogger } from '../lib/ZBLogger';
export declare class JobPoller extends EventEmitter {
    pollInterval: number;
    pollHandle: NodeJS.Timeout;
    gRPCClient: any;
    activeJobs: number;
    maxActiveJobs: any;
    taskType: any;
    logger: ZBLogger;
    id: any;
    timeout: any;
    constructor({ pollInterval, gRPCClient, taskType, maxActiveJobs, logger, id, timeout, }: {
        pollInterval: any;
        gRPCClient: any;
        taskType: any;
        maxActiveJobs: any;
        logger: any;
        id: any;
        timeout: any;
    });
    close(): void;
    private makeFailureHandler;
    private makeSuccessHandler;
    private activateJobs;
}
