import { Loglevel, ZBWorkerLoggerOptions } from './interfaces';
export declare class ZBLogger {
    loglevel: Loglevel;
    private colorFn;
    private taskType;
    private id?;
    private stdout;
    private colorise;
    private pollInterval;
    constructor({ loglevel, color, id, namespace, stdout, taskType, colorise, pollInterval, }: ZBWorkerLoggerOptions & {
        id?: string;
        colorise?: boolean;
    });
    info(message: any, ...optionalParameters: any[]): void;
    error(message: any, ...optionalParameters: any[]): void;
    debug(message: any, ...optionalParameters: any[]): void;
    log(message: any, ...optionalParameters: any[]): void;
    private makeMessage;
    private _colorise;
}
